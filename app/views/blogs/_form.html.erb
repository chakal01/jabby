<%= bootstrap_form_for(@blog, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-8" ) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :url, label: "URL" %>
  <%= f.text_field :name, label: "Nom" %>
  <%= f.text_area :content, label: "Texte d'intro" %>
  <%= f.select :camp_id, @camp_list, { label: "Camp relié" } %>
  <%= f.form_group do %>
    <%= f.submit value: "Créer", class: "btn btn-default col-sm-4" %>
  <% end %>


<% end %>
