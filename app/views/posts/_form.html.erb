<%= bootstrap_form_for(@post, url: blog_posts_path(@blog), layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-8") do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.text_field :date, value: @post.date||@placeholder_date %>
  <%= f.text_field :title, label: "Titre" %>
  <%= f.text_area :content, label: "Texte", rows: "10" %>
  <%= f.form_group do %>
    <%= f.submit value: "Créer", class: "btn btn-default col-sm-4" %>
  <% end %>

<% end %>
